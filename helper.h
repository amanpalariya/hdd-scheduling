#ifndef CS303_HELPER_H
#define CS303_HELPER_H

int randint(int min, int max);

float mean(long* ar, int n);
float min(long* ar, int n);
float max(long* ar, int n);
float standard_deviation(long* ar, int n);

#endif